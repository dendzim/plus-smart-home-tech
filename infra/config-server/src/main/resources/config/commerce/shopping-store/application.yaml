server:
  port: 8083
spring:
  output:
    ansi:
      enabled: ALWAYS
  datasource:
    url: jdbc:postgresql://localhost:5432/test?currentSchema=store
    username: ${DB_USERNAME:denispanasuk}
    password: ${DB_PASSWORD:password}
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
eureka:
  instance:
    hostname: localhost
    preferIpAddress: false
    instance-id: shopping-store:${random.value}