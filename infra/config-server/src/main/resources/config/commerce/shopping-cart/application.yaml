server:
  port: 8082
spring:
  output:
    ansi:
      enabled: ALWAYS
  jpa:
    hibernate:
      ddl-auto: create-drop
    datasource:
      url: jdbc:postgresql://localhost:5432/cart
      username: postgres
      password: password
      driver-class-name: org.postgresql.Driver
eureka:
  instance:
    hostname: localhost
    preferIpAddress: false
    instance-id: shopping-cart:${random.value}